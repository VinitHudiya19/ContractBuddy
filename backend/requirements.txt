# ---- Web framework ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
sse-starlette==2.1.3

# ---- Config / validation ----
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# ---- Database (async Postgres) ----
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
greenlet==3.1.1

# ---- Cache / rate limiting ----
redis==5.2.1

# ---- Vector DB ----
qdrant-client==1.12.2

# ---- Auth / security ----
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Pinned <4.1: passlib 1.7.4 reads bcrypt.__about__.__version__, removed in 4.1+.
# 4.0.1 keeps hashing identical while silencing the spurious version warning.
bcrypt==4.0.1

# ---- Document parsing ----
pymupdf==1.25.1
python-docx==1.1.2

# ---- Embeddings / rerank (local, free) ----
sentence-transformers==3.3.1

# ---- LLM providers (pluggable; all optional at runtime) ----
groq==0.13.1
google-generativeai==0.8.3
openai==1.58.1
anthropic==0.42.0
httpx==0.28.1

# ---- Retrieval helpers ----
rank-bm25==0.2.2

# ---- Reliability ----
tenacity==9.0.0

# ---- Observability ----
prometheus-client==0.21.1
structlog==24.4.0

# ---- Testing ----
pytest==8.3.4
pytest-asyncio==0.25.0
aiosqlite==0.20.0
